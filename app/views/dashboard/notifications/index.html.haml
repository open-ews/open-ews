.card
  .card-header
    .card-actions
      %a.btn
        %i.icon.ti.ti-filter
        Filter
      %a.btn
        %i.icon.ti.ti-file-export
        Export

  = render("shared/resources_table", resource_class: Notification, columns: [:beneficiary_id, :broadcast_id, :created_at])
  = render("shared/pagination", resources: resources)
